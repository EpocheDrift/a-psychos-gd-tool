/**
 * These literals are injected by vite.config.ts from the selected build mode.
 * They intentionally cannot be overridden by ambient VITE_* environment
 * variables.
 */
declare const __GFX_AGENT_BUILD__: boolean;
declare const __GFX_AGENT_ALLOWED_ORIGIN__: string;
