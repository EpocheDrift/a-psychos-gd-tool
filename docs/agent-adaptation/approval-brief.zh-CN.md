# Agent-ready v1 中文审批简报

状态：**已正式批准，可进入 Ready for review；尚未合并**

批准日期：**2026-07-27**

对应对象：

- PR [#2](https://github.com/EpocheDrift/a-psychos-gd-tool/pull/2)
- 分支 `agent/agent-ready-v1`
- Agent-ready v1（PR 0–PR 8）

这不是英文技术文档的逐篇翻译。它只保留当前需要审批的决定、推荐选项、
已掌握的证据，以及可以后置的风险。目标是在大约 10 分钟内完成一次有质量的
决策，而不是要求审批人重新参与整个实现过程。

## 30 秒结论

项目所有者的最终决定是：

> **正式批准 Agent-ready v1 的技术方案、安全边界和产品范围。取消后续完整
> 能力 sweep，不再把额外人工验收或远端 CI 记录作为本次批准的前置条件。
> PR 可以从 Draft 转为 Ready for review；本次决定不自动执行合并或发布。**

决定依据：

- 工程正确性和安全性已经有完整自动化证据；
- 已完成一次真实 Session B：Codex 通过 production MCP 上传用户素材、提交
  原子设计事务、等待精确 revision 渲染、检查 PNG，并根据首版预览自主修正
  `Recolor` 不透明背景问题；
- 项目所有者接受当前剩余风险，并明确选择不再进行完整 31 节点能力 sweep；
- 当前 head 仍没有 GitHub Actions 远端运行记录。这个事实继续保留为
  merge/release 风险，但不再阻塞所有者批准或 Ready for review。

## 你现在实际在批准什么

批准后，代表你接受：

1. Agent 通过受限 MCP 和语义命令操作现有节点图，而不是模拟鼠标拖小插口。
2. 普通编辑使用原子事务、revision 冲突检查、request ID 幂等和安全 revert。
3. Agent 可以读文档、改图、上传受限素材、渲染和查看预览。
4. 这个设计 MCP 本身不新增或转授文件系统、任意 URL、shell、任意
   JavaScript、浏览器 CDP/导航和整项目替换能力；Codex、Claude Code 等宿主
   原本拥有什么权限，继续由各自的沙箱和用户批准策略管理。
5. v1 的目标是“可靠执行受限设计工作流”，不是“自动成为完整美术总监”。

这次批准**不代表**：

- 批准立即合并或发布；
- 批准 RMBG-1.4 用于商业场景；
- 批准云端多人或多租户 Agent 服务；
- 承诺所有浏览器、GPU 和操作系统上的像素完全一致；
- 承诺 Agent 已具备无边界的自主审美或自然语言设计能力。

## 需要你拍板的 4 个决定

### 决定 1：v1 能力范围是否够用

当前范围：

- 能发现节点、参数和 typed sockets；
- 能一次创建并连接多节点图；
- 能等待指定 revision 的真实渲染；
- 能看预览和可量化图像指标；
- 能处理人工冲突、错误连线、渲染失败和响应丢失。

我的推荐：**接受。**

原因：这已经构成一个可实际使用的 Agent 控制面。更高阶的自动排版模板、
subgraph duplication 等 helper 暂时没有真实 trace 证明值得扩权。

### 决定 2：是否接受“窄 MCP、宿主权限另管”的边界

这里必须区分两层权限：

1. **宿主 Agent 的权限**：Codex 或 Claude Code 可能本来就有 shell、工作区
   文件、网络或其他浏览器工具。这些权限来自运行它们的宿主环境和你的批准，
   不由本项目授予，也不由本项目撤销。
2. **Graphic Design MCP 的权限**：这个 MCP 只提供经过 allowlist 的设计工具，
   例如读文档、提交事务、等待渲染、获取预览和管理受限素材。它不会把一次
   设计连接变成 shell、通用文件系统、任意网络、CDP 或任意页面执行入口。

换句话说，我们保证的是：

- 连接这个 MCP 不会让一个原本受限的 Agent 获得新的通用系统权限；
- 文档内容或 prompt 不能通过 MCP 自己提升 scope；
- Agent 不能借 `gfx_*` 工具读取任意本地文件、访问任意 URL 或控制整个浏览器；
- 设计应用内部只接受版本化、校验过的命令。

我们**不保证**：

- Codex 或 Claude Code 在它自己的宿主环境里没有 shell；
- 宿主已经授予的文件/网络权限会被这个 MCP 收回；
- 一个同时拥有其他强力工具的 Agent 只能通过本 MCP 行动。

如果需要“Agent 只能使用设计 MCP”，还必须在 MCP 之外把宿主 Agent 放进
独立 sandbox/container，只给它这个 MCP 连接。那属于部署策略，不是本项目
自身可以完成的安全边界。

我的推荐：**接受，先不要放宽。**

原因：现有三类创作任务都能通过这个窄 MCP 一次原子完成。等真实使用出现
明确阻碍，再针对单一设计能力增加窄工具，比在 MCP 内加入通用 shell、文件
或浏览器控制更容易审计。宿主 Codex/Claude Code 是否可以同时使用其他工具，
应作为另一项运行时权限决策单独管理。

### 决定 3：RMBG-1.4 的 A 路线是否继续

当前约束：

- 模型只能走固定、校验过的本地缓存；
- 首次下载需要独立的人类许可确认；
- Agent 看不到模型 URL 或文件路径；
- 当前披露要求商业使用另行获得协议。

我的推荐：**继续 A 路线，但把它明确标为非商业/内部试用能力。**

如果下一阶段要商业发布，模型许可必须成为独立发布门，不应由本次代码审批
顺带放行。

### 决定 4：PR 状态与合并时机

最终决定：**现在可以改为 Ready for review；合并仍是单独动作。**

- [x] 已完成一次真实 Agent MCP 素材海报闭环，并检查两轮视觉结果；
- [ ] 完整 Web UI / 31 节点能力 sweep（所有者明确豁免，不声称已完成）；
- [ ] 当前 head 的 GitHub Actions 远端通过记录（所有者明确豁免为批准条件，
  不声称已通过）。

Ready for review 只表示所有者接受当前 v1 方案和风险，可以进入正常代码评审。
它不等于自动合并、生产发布或商业许可放行。

## 你不需要重新审的内容

除非你想深入技术细节，否则不需要重新逐条检查：

- 每个 schema 字段；
- 527 个应用测试和 87 个 companion 测试的单独实现；
- 所有 WebSocket、rate-limit 和 replay 内部代码；
- 3,000 多行 PR8 diff；
- 每一份英文设计文档。

这些已经经过自动化门和独立只读审计。你更应该把注意力放在：

- Agent 做出来的结果是不是你愿意用；
- 人类和 Agent 交替编辑时是否自然；
- 当前权限是不是符合你的风险偏好；
- 非商业模型限制是否符合下一步计划。

## 最小有效证据

| 证据 | 当前结果 | 它说明什么 |
| --- | --- | --- |
| 应用测试 | 527/527 | 编辑、事务、渲染、资产和 UI 基础行为没有已知回归 |
| Companion 测试 | 87/87 | MCP、配对、模型、stdio 和 transport 契约稳定 |
| Agent 评测 | 7/7 场景，49 次调用 | Agent 能完成真实创作和恢复闭环 |
| 恢复能力 | 4/4 | 人工冲突、坏计划、渲染失败、响应丢失均可恢复 |
| 视觉证据 | 3 张已审 PNG | 输出不是只在 JSON 层“看起来成功” |
| 浏览器验收 | 9/9 smoke，语义 UI 50/50 | 人类 UI 和真实 WebGPU 路径保持可用 |
| 独立审计 | 无 blocker | 安全、错误契约、runner、golden 和文档已复核 |
| 真实 Session B | 用户 JPEG、63 条原子设计命令、2 轮 PNG 视觉闭环 | 外部 Agent 能用生产 MCP 自主完成并修正一张真实素材海报 |

## 风险压缩表

| 风险 | 是否影响当前正式批准 | 建议 |
| --- | --- | --- |
| GitHub Actions 没有当前 head 的运行记录 | 不再阻塞所有者批准；仍是 merge/release 风险 | 评审或发布方可自行决定是否补跑 |
| RMBG-1.4 商业许可 | 不阻塞内部/非商业试用；阻塞商业发布 | 商业化前独立解决许可 |
| CI 不能代表所有硬件 GPU | 不阻塞 v1 代码审批 | 发布前在目标硬件做一次验收 |
| `Event.isTrusted` 不是物理本人证明 | 不阻塞本地单用户模式 | 高安全部署再考虑 OS/WebAuthn 确认 |
| 共享素材缓存上限 256 MiB | 不阻塞当前范围 | 真实使用出现容量压力后再设计回收协议 |
| 现有依赖安全告警，零 critical | 不阻塞本次范围 | 单独开依赖升级/替换任务，不在本 PR 盲改 |

## 可选后续实操

项目所有者已决定不继续完整能力测试，所以下面的 Session A/B 只作为未来回归、
演示或产品研究参考，**不再是本次批准、Ready for review 或所有者签字的门槛**。
如以后重新执行，建议拆成两个独立 session，不和代码 review 混在一起。
第一次使用时可直接跟随
[中文 Web UI 与 Agent MCP 入门](../getting-started.zh-CN.md)；对应英文版本是
[Getting started](../getting-started.md)。

### Session A：人类 Web UI，约 15–20 分钟

1. 打开现有项目，修改文字、颜色和 frame。
2. 新增/连接几个节点，测试 Undo/Redo。
3. Save Project，再 Load Project。
4. 上传一个真实素材，确认保存、重开和预览均正常。

通过标准：

- 没有数据丢失；
- 原有人类编辑手感没有明显变差；
- 保存/载入和 Undo/Redo 符合预期；
- 错误提示能让人理解下一步怎么处理。

### Session B：Agent MCP，约 20–30 分钟

建议实际做三个贴近使用的案例：

1. **海报标题**：让 Agent 创建 Text → Outline → Warp → Rasterize →
   Recolor → Output，并根据预览再改一次颜色或弯曲程度。
2. **环形字标**：让 Agent 用 Split + Math Function + Place 做一个环形
   文字构图，再要求它调整半径和字距。
3. **人机交替编辑**：Agent 先读取并规划；你在 UI 中临时改一个 layer 或
   frame；再让 Agent 继续，确认它会重新读取而不是覆盖你的修改。

可选第四例：

4. **真实素材/去背景**：在你明确接受模型许可后，上传自己的图片并运行
   Remove Background；检查模型权限、失败提示和恢复流程。

通过标准：

- Agent 不需要鼠标坐标或内部 store；
- 结果可见且不是空白；
- 第二轮修改基于当前 revision；
- 人工插入修改不会被覆盖；
- 出错后 Agent 能解释并继续，不需要重启整个项目。

如果未来执行这两次可选 session，观察结果应单独记录为“产品验收证据”，
不要塞进技术测试日志；再据此决定是否需要新增 helper、调整权限或修改默认
工作流。

## 最终审批结果

- [x] **A — 正式批准（已选择）**：接受技术方案、安全边界、v1 范围和已披露
  剩余风险；取消额外能力 sweep，将 PR 转为 Ready for review。
- [ ] **B — 暂缓**：当前范围或权限仍不符合预期，先列出具体差距。
- [ ] **C — 拒绝**：不采用当前 Agent MCP 架构。

当前决定：**A — 正式批准。**

仍然不在本次批准范围内：自动合并、生产发布、RMBG-1.4 商业使用、云端多租户
Agent 服务，以及未执行测试的“已通过”声明。

## 需要深挖时再看的附件

- [Agent 评测与恢复场景](./evaluation-suite.md)
- [完整交付证据](./delivery-checklist.md)
- [目标架构](./architecture.md)
- [实施计划与 Definition of Done](./implementation-plan.md)
- [历史基线风险审计](./readiness-audit.md)
