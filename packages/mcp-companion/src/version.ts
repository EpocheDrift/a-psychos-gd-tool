// Runtime surfaces use one version constant. scripts/version-check.mjs keeps
// it aligned with both workspace package manifests and release tags.
export const COMPANION_VERSION = '0.1.0-alpha.0' as const;
